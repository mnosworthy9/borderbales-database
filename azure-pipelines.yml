trigger:
- master

pool:
  vmImage: ubuntu-latest

variables:
- group: Database
- group: Docker
- name: imageTag
  value: '$(DatabaseContainer)-$(Build.BuildId)'
- name: dockerHubServiceConnection
  value: 'Docker Hub'

steps:
- task: Docker@2
  displayName: 'Build Docker image Database'
  inputs:
    command: 'build'
    containerRegistry: $(dockerHubServiceConnection)
    repository: '$(DockerUsername)/$(DockerRepo)'
    Dockerfile: '**/Dockerfile'
    tags: |
      $(imageTag)
    arguments: '--build-arg DB_USER=$(DB_USER) --build-arg DB_PASS=$(DB_PASS) --build-arg DB_NAME=$(DB_NAME) --build-arg DB_PORT=$(DB_PORT) --platform=linux/arm64'

- task: Docker@2
  displayName: 'Push Docker image to Docker Hub'
  inputs:
    command: 'push'
    containerRegistry: $(dockerHubServiceConnection)
    repository: '$(DockerUsername)/$(DockerRepo)'
    tags: |
      $(imageTag)
